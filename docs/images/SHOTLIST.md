# Image shot list

Drop screenshots here (PNG, ~1200px wide). The README references these exact names:

- `dashboard.png`      - the mission-control dashboard (the hero shot). Use a
                         redacted/demo view: crop or blur any private domain,
                         IP, or real activity. This is the single most important image.
- `telegram-brief.png` - (optional) a chat showing you messaging the agent and it
                         replying with a morning brief. Redact personal content.
- `vault-search.png`   - (optional) a semantic_search result, or the tasks.md mirror.

Keep them clean and legible. No live URLs, IPs, or personal data in frame.
